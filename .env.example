# Canonical production URL (set for local or non-Vercel deployments)
# Local: http://localhost:3000
# Production: https://your-domain.com
SITE_URL=

# Set your machine's LAN IP to test on mobile over the local network.
DEV_ORIGIN=
